# DESIGN.md — Lab 页面设计上下文（桌面优先的本地研究工具）

> 日期：2026-09-16（因子 Web MVP Task 1 提取）
>
> 本文件从 `internal/lab/web/lab/index.html` 提取当前视觉事实，作为后续页面改动的
> 基线约束。改版时延续本文件，不引入新组件库、在线字体或营销化视觉。

## 1. 产品定位

- **桌面优先的本地研究工具**（127.0.0.1，单用户），不是营销网站，不是交易终端。
- 目标密度：紧凑、可审计。信息优先于装饰；不使用闪烁、渐变动画、大数字排版。
- 用户：熟悉 A 股策略、不一定熟悉因子研究术语的个人研究者。
- 简单模式表单、因子研究页均遵循本基线；高级脚本编辑器保持现状。

## 2. 视觉事实（自当前 index.html 提取）

### 2.1 颜色（CSS 变量）

| 变量 | 值 | 用途 |
|---|---|---|
| `--bg` | `#f5f6fa` | 页面背景（浅灰） |
| `--bg2` | `#fff` | 工作区/卡片背景（白） |
| `--ink` | `#1a1a2e` | 主文字 |
| `--muted` | `#6b7280` | 解释、单位、次要文字 |
| `--rule` | `#e5e7eb` | 边框、分隔线 |
| `--accent` | `#3b82f6` | 当前选择、主操作（蓝） |
| `--accent2` | `#1e293b` | 卡片标题、深色强调 |
| `--red` | `#ef4444` | 涨/盈利（A 股红涨语义） |
| `--green` | `#22c55e` | 跌/亏损（A 股绿跌语义） |
| `--ok` | `#10b981` | 成功状态 |
| `--warn` | `#f59e0b` | 琥珀：数据覆盖、成交口径、研究限制提示 |

颜色规则（设计文档 §6.1）：

- 蓝 = 当前选择和主操作；中性灰 = 解释、单位、样本范围；
- 红涨绿跌只用于收益与 A 股行情语义，且必须同时给出文字或符号，不用颜色单独表达好/坏；
- 琥珀 = 数据覆盖（Coverage skipped）、成交口径提示、研究限制说明；
- header 保持 `linear-gradient(135deg,#1e293b,#334155)` 深色条。

### 2.2 字体与字号

- 界面：`-apple-system, "Microsoft YaHei", "PingFang SC", sans-serif`，正文 `14px`，行高 `1.6`。
- 等宽（脚本编辑器、消息、数字审计场景）：`Consolas, "Courier New", monospace`，`13px`。
- 层级：h1 `18px/700`；卡片 h3 `14px`（底边 `2px` 蓝色下划线）；表格 `13px`；hint/msg `12px`。
- 不新增在线字体依赖；数字直接用系统字体渲染。

### 2.3 间距与边框

- 页面：`main{padding:20px 24px;max-width:1400px;margin:0 auto}`；header `14px 24px`。
- 卡片：白底、`1px solid var(--rule)`、圆角 `10px`、内边距 `16px`；卡片间距 `16px`。
- 控件：输入/下拉高 `32px`、圆角 `6px`、边框 `1px solid var(--rule)`；按钮内边距约 `8px 20px`。
- 表单行：`display:flex;gap:8px`，label 固定宽约 `88px`，可换行。
- 焦点：输入 `outline:2px solid var(--accent)`；所有可交互元素必须有可见焦点样式。

### 2.4 布局与密度

- 四 Tab 单页：`① 策略与运行` / `② 组合对比` / `③ 交易明细` / `④ 因子研究`。
- Tab1 `grid2`：内容列 `1fr` + 配置列 `340px`。
- 表格：全宽、紧凑单元格（`8px 10px`）、表头浅灰吸顶；明细容器限高滚动（约 `520px`）。
- 图表（ECharts）：高度 `340px`(Tab2) / `480px`(K线钻取) / `360px`(IC)；图表标题 `13px` 左上。
- 状态区：浅蓝状态条 `#f0f4ff` + 进度条；消息区 `12px` 等宽、限高滚动。

## 3. 控件所有权（MVP 合同）

| 能力 | 归属 | 约束 |
|---|---|---|
| Tab | **应用自有** | `role=tablist/tab/tabpanel`、方向键左右切换、Home/End、焦点管理、选中态同步（`aria-selected`、`tabindex` 轮换）；Tab 状态写入 URL `?tab=` |
| 下拉选择 | **原生 `<select>`** | 接受浏览器弹层外观；不实现自定义下拉、不承诺弹层宽度圆角 |
| 年份输入 | **原生 `input[type=number]`** | 只输年份；交易明细日期筛选保持原生 `input[type=date]` |
| 表单校验 | **应用自有** | 表单 `novalidate`；错误用字段关联文本展示（非浏览器校验气泡），聚焦第一个错误字段 |
| 运行/异步状态 | **应用自有状态区** | 状态区使用 `aria-live="polite"`；进度、错误、重试均有文本，不依赖颜色 |
| 滚动条 | **浏览器原生** | 不隐藏滚动条；表格横向滚动自身解决，不用表格高度约束配置表单 |
| 脚本 textarea | 保持现状 | 高级模式不在本 MVP 改动范围（Task 9 仅固定高度、移除 resize、提供展开按钮） |

## 4. 异步状态要求（每个异步区域必须覆盖）

1. 初始引导（空态文案）；
2. 加载中与可停止状态（进度数字 + 停止按钮）；
3. 无有效样本（如无 quintiles，不画空柱、不出现 NaN/undefined）;
4. 部分股票加载失败（Coverage skipped > 0，醒目但不夸张的琥珀提示，失败项可展开）;
5. 失败及重试（保留表单值，给出可操作修正说明）;
6. 完成（自动进入对应结果区）;
7. 已加入策略但尚未回测（简单配置待确认态）。

按钮运行中保持尺寸不变并阻止重复提交；任务互斥（409）提示"已有回测或因子分析正在运行"，不透出原始 Go 错误。

## 5. 范围外既有问题（本次不修）

以下为提取基线时观察到的现状问题，不属于因子 Web MVP 修复范围（避免扩大扫描制造无关阻塞）：

- 既有 Tab1 脚本编辑器、Tab2/3 图表与明细的样式细节一致性；
- `output/reports/`、`core/analyze_html.go` 等非 Lab 页面的 HTML 产物样式；
- ECharts CDN 加载失败时的离线降级（本地工具，可接受）。

审计范围严格限定在 `internal/lab/web/lab`（见 `premium-ui.json`）。

## 6. 审计基线状态

- **审计工具在本环境不可用**（仓库与工具链中未提供 Premium UI 审计命令），基线审计**未运行**。
- 按实施计划降级路径处理：`premium-ui.json` 仅作为审计范围与决策的声明基线；
  "现有问题分为本次必须修复 / 范围外既有问题"的划分由 §5 的人工提取代替，**不视为审计通过**。
- Task 10 端到端回归时若审计工具可用，再以本文件为基线补跑并更新本节。
