# DESIGN.md — Lab 页面设计上下文（桌面优先的本地研究工具）

> 日期：2026-09-16（因子 Web MVP Task 1 提取）
>
> 本文件以 `internal/lab/web/lab/index.html` 的实际实现为运行时映射，作为后续页面改动的
> 基线约束。改版时延续本文件，不引入前端框架、在线字体或营销化视觉。

## 1. 产品定位

- **桌面优先、窄屏可用的本地研究工具**（127.0.0.1，单用户），不是营销网站，也不假装成实时交易终端。
- 视觉 North Star 是“冷静的研究工作台”：像实验记录册与行情数据表的交叉体，紧凑、可审计，信息优先于装饰。
- 记忆点是贯穿页面的四段研究路径（因子 → 配置 → 对比 → 明细）；数字只有在真实流程顺序中使用。根路径默认进入因子研究，已带 `?tab=` 的深链接仍恢复指定页。
- 用户：熟悉 A 股策略、不一定熟悉因子研究术语的个人研究者。
- 简单模式表单、因子研究页均遵循本基线；高级脚本编辑器保持现状。

## 2. 视觉事实（自当前 index.html 提取）

### 2.1 颜色（CSS 变量）

| 变量 | 值 | 用途 |
|---|---|---|
| `--bg` | `#f2f5fa` | 页面背景（雾蓝灰） |
| `--bg2` | `#fff` | 工作区/卡片背景（白） |
| `--ink` | `#172234` | 主文字 |
| `--muted` | `#67748a` | 解释、单位、次要文字 |
| `--rule` | `#dbe2ec` | 边框、分隔线 |
| `--accent` | `#2864e8` | 当前选择、主操作（钴蓝） |
| `--accent2` | `#15243a` | 卡片标题、深色强调 |
| `--navy` | `#132238` | 页头与研究工作台识别色 |
| `--red` | `#df3f4f` | 涨/盈利（A 股红涨语义） |
| `--green` | `#159668` | 跌/亏损（A 股绿跌语义） |
| `--ok` | `#0f9f6e` | 成功状态 |
| `--warn` | `#d59020` | 琥珀：数据覆盖、成交口径、研究限制提示 |

颜色规则（设计文档 §6.1）：

- 蓝 = 当前选择和主操作；中性灰 = 解释、单位、样本范围；
- 红涨绿跌只用于收益与 A 股行情语义，且必须同时给出文字或符号，不用颜色单独表达好/坏；
- 琥珀 = 数据覆盖（Coverage skipped）、成交口径提示、研究限制说明；
- header 使用 `--navy` 到 `--navy-2` 的深色面，并以低对比数据网格表达研究语境；正文网格只能作为背景纹理，不能干扰阅读。

### 2.2 字体与字号

- 界面：`"Segoe UI Variable Text", "Microsoft YaHei UI", "PingFang SC", system-ui, sans-serif`，正文 `14px`，行高 `1.6`。
- 等宽（脚本编辑器、消息、数字审计场景）：`Consolas, "Courier New", monospace`，`13px`。
- 层级：h1 `20px/720`；卡片 h3 `15px`（左侧钴蓝索引线）；表格 `12px`；hint/msg `12px`。
- 不新增在线字体依赖；数字直接用系统字体渲染。

### 2.3 间距与边框

- 页面：`main{padding:24px;max-width:1448px;margin:0 auto}`；header 最小高度 `84px`。
- 卡片：白底、`1px solid var(--rule)`、圆角 `14px`、内边距 `20px`；阴影保持低对比，不能形成浮夸卡片墙。
- 控件：输入/下拉高 `36px`、圆角 `9px`、边框 `1px solid #ccd5e2`；按钮最小高 `36px`。
- 表单行：`display:flex;gap:8px`，label 固定宽约 `88px`，可换行。
- 焦点：输入 `outline:2px solid var(--accent)`；所有可交互元素必须有可见焦点样式。

### 2.4 布局与密度

- 四 Tab 单页以 `01`～`04` 表示真实研究路径：因子研究 / 策略与运行 / 组合对比 / 交易明细；每段配一条任务解释，而不是装饰性编号。
- “策略与运行”（内部 `tab1`）的 `grid2`：内容列 `1fr` + 配置列 `360px`。
- 表格：全宽、紧凑单元格（`8px 10px`）、表头浅灰吸顶；明细容器限高滚动（约 `520px`）。
- 图表（ECharts）：高度 `340px`(Tab2) / `480px`(K线钻取) / `360px`(IC)；图表标题 `13px` 左上。
- 状态区：`main` 顶部只有一个跨 Tab 的“批量任务”状态条，因子分析与策略回测共用；蓝金进度条显示股票完成数与百分比，消息区 `12px` 等宽、限高滚动。
- `≤900px` 时双栏和双图退为单栏；`≤560px` 时 Tab 保留编号和中文短标题，表单标签独占一行。任何宽度不得用 `overflow:hidden` 掩盖页面内容。

### 2.5 动效

- GSAP Core 仅用于首屏品牌与研究路径进入、Tab 面板切换、任务状态条出现和分析结果揭示；不做循环装饰、滚动劫持或行情闪烁。
- 动效只使用 `autoAlpha`、`x/y` 等合成属性，时长 `0.30s`～`0.45s`，缓动 `power2.out`，列表揭示间隔约 `0.045s`。
- 必须通过 `gsap.matchMedia()` 尊重 `prefers-reduced-motion`；GSAP CDN 不可用时功能与布局不能受影响。

## 3. 控件所有权（MVP 合同）

| Capability | Canonical owner | Source of truth | Allowed variants | Verification |
|---|---|---|---|---|
| Tab | 应用自有 WAI-ARIA Tab | `DESIGN.md` + `index.html` | 四段研究路径 | 键盘方向键、Home/End、焦点、URL `?tab=` |
| Select/Listbox | 浏览器原生 `<select>` | `DESIGN.md` | native | 键盘选择与原生弹层 |
| Date | 浏览器原生输入 | `DESIGN.md` | `number` 年份 / `date` 明细筛选 | 本地格式与键盘输入 |
| Form | 页面表单与 `buildSpec()` 校验 | `DESIGN.md` + API 合同 | simple / advanced | 字段错误、首错聚焦、服务端复核 |
| Scrollbar | 浏览器原生滚动条 | `DESIGN.md` | 页面 / 表格局部滚动 | 1280 与窄屏浏览器检查 |
| Status | 应用自有全局批量任务条 | `DESIGN.md` + `/api/status` | preparing / running / done / error / stopped | 原生 `progress`、`aria-live`、错误恢复与停止 |
| Textarea | 页面高级脚本编辑器 | `DESIGN.md` | fixed / expanded | 固定高度、无拖拽 resize、展开按钮 |

因子研究的字段语义：因子选择器只显示稳定的类型名（如“`N日动量`”），参数字段使用目录提供的中文名称（如“回看天数”），概念卡显示带当前参数的实例名（如“`N日动量(60)`”）。未来收益窗口是独立的评价周期，不得与因子历史计算窗口合并或重复显示。无参数的单根 K 线因子隐藏窗口输入。

## 4. 异步状态要求（每个异步区域必须覆盖）

1. 初始引导（空态文案）；
2. 加载中与可停止状态（总量未就绪时是不定进度；开始逐股处理后显示完成数、总数、百分比和当前代码）；
3. 无有效样本（如无 quintiles，不画空柱、不出现 NaN/undefined）;
4. 部分股票加载失败（Coverage skipped > 0，醒目但不夸张的琥珀提示，失败项可展开）;
5. 失败及重试（保留表单值，给出可操作修正说明）;
6. 完成（自动进入对应结果区）;
7. 已加入策略但尚未回测（简单配置待确认态）。

简单配置中的“基础策略”是可选条件组：空值表示不附加任何预设 Buyer，买入仅由 1～4 个因子条件决定；此时“全部样本”只作为对照基准，不是隐式策略。选择预设后，预设条件与因子条件固定按 AND 组合。原生 Select 是本项目已确认的 canonical owner，空选项必须始终可重新选择。

按钮运行中保持尺寸不变并阻止重复提交；任务互斥（409）提示"已有回测或因子分析正在运行"，不透出原始 Go 错误。
分析与回测不得在各自面板重复实现进度文案或局部进度条；统一消费 `/api/status`，共享同一个停止入口。进度查询中断时不得宣称任务失败，应提示后台状态未知并提供“重试读取”。

### 4.1 候选因子库 UI 合同（2026-09-17）

- 保存入口（canonical owner：`index.html` + `POST /api/factor-candidates`）：因子分析完成后面板内“保存为候选因子”表单；未运行分析或旧版报告（缺 analysisId/实现版本）时按钮禁用并提示“重新运行分析”。等频（quantile）默认“仅保存观察结果”且阈值保持为空——分位边界只作展示，不得自动转成阈值；固定区间（bins）仅展示断点建议。range 阈值按百分比填写（5 = 5%），提交 ÷100。分析刷新后表单重置，requestId 重置保证幂等语义清晰。
- 候选列表（“我的候选因子”卡）：列 = 名称/因子/用途/证据区间/状态/兼容/操作；状态 chip（候选/已归档）与兼容 chip（可复用/实现已更新）；observe 候选不提供“加入策略”。
- 候选管理自 2026-09-17 起位于独立“候选因子”tab（`?tab=candidates`，第 5 个 tab）：列表卡（“我的候选因子”）与两个候选 dialog 均不在因子研究页内；dialog 位于 main 顶层（tab panel 之外），不得移回任何 panel——hidden 祖先（display:none）内 showModal 不渲染。保存表单留在因子研究页分析结果区，保存成功后出现“查看候选 →”跳转（新分析渲染时重置隐藏）。
- 证据 dialog（`#candEvDlg`）只读展示 analysisId、分析版本、证据 SHA-256、IC 摘要与完成时间，并声明“样本内研究记录不可变”；编辑 dialog（`#candEditDlg`）预填当前修订，保存追加修订并显示“当前修订 vX”，409 冲突提示刷新后重试。
- 归档：行操作“归档/恢复”+ 头部“查看归档”切换；加入策略把候选阈值写入策略条件并打“尚未回测”chip，遇到同 kind+days 条件时先 confirm 是否替换（不静默覆盖），4 条上限时拒绝并聚焦。
- 列表加载失败用 `#candLoadErr` 可重试警示条；保存/修订沿用全局按钮禁用防重复提交约定。

## 5. 范围外既有问题（本次不修）

以下为提取基线时观察到的现状问题，不属于因子 Web MVP 修复范围（避免扩大扫描制造无关阻塞）：

- `output/reports/`、`core/analyze_html.go` 等非 Lab 页面的 HTML 产物样式；
- ECharts / GSAP CDN 加载失败时的离线图表与动效降级（本地工具，功能页面仍须可操作）。

审计范围严格限定在 `internal/lab/web/lab`（见 `premium-ui.json`）。

## 6. 审计基线状态

- `premium-ui.json` 使用当前 Premium UI 审计清单，范围只包含 `internal/lab/web/lab`，控件所有权以上方 Canonical UI Map 为准。
- 2026-09-16 运行 `audit_project.py --mode strict --config premium-ui.json --no-write`：0 errors / 0 warnings。
- 静态审计不证明浏览器运行时行为；键盘 Tab、窄屏布局和减少动态效果仍需以实际浏览器检查作为证据。
